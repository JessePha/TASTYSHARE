import React from "react";
import { View, Text } from "react-native";

const NotificationView = () => {
  return (
    <View>
      <Text>Notication</Text>
    </View>
  );
};

export default NotificationView;
